import 'package:flutter/material.dart';

class Textfield7 extends StatelessWidget {
  const Textfield7({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: TextField(
        keyboardType: TextInputType.number,
      ),
    );
  }
}
